# UdpFileTransfer 向け AI・開発ルール

このリポジトリは、送信側から受信側への片方向 UDP 通信だけでファイル転送を成立させる Java 8 CLI を、小さく追跡可能な構成で保守します。

## 正本

- 文書体系・仕様ID・受入条件・追跡規則: [`docs/README.md`](docs/README.md)
- 利用者向け説明: [`README.md`](README.md)
- 外部仕様: [`docs/EXTERNAL_DESIGN.md`](docs/EXTERNAL_DESIGN.md)
- 内部設計: [`docs/INTERNAL_DESIGN.md`](docs/INTERNAL_DESIGN.md)
- ビルド・テスト・リリース: [`docs/OPERATIONS.md`](docs/OPERATIONS.md)
- 重要な設計判断: [`docs/adr/`](docs/adr/)

## 仕様駆動開発

機能、外部挙動、内部構造を変更するときは、製品コードを先に変更して仕様を後付けしません。原則として次の順で作業します。

1. `docs/README.md`、外部設計、内部設計、関連ADR、関連テスト、製品コードの順に現状を確認する。
2. 外部から見える挙動が変わる場合は `EXTERNAL_DESIGN.md` と対応する `EXT-*` を先に変更する。
3. 回帰リスクのある外部仕様には、実装詳細に依存しない `AC-*` を定義・更新する。
4. 必要な内部方式を `INTERNAL_DESIGN.md` と `INT-*` に反映する。
5. 代替案と理由を将来も残す価値がある判断はADRに記録する。
6. `EXT -> AC -> INT -> test` の対応を確認し、テストを変更・追加する。
7. 最後に製品コードを変更する。
8. README、OPERATIONSなどへの影響を反映する。
9. 仕様、設計、テスト、実装が一致していることを確認する。

内部設計を検討した結果、外部仕様が不適切だと分かった場合は、実装へ合わせて黙って挙動を変えず、外部設計へ戻って仕様変更を明示します。

## 正本と矛盾

- テストと製品コードは仕様の検証・実現物であり、仕様書と矛盾した場合に自動的な正本にはしない。
- 文書間の矛盾は、実装から推測して埋めず、どの契約を正とするか判断して正本側を直す。
- 未確定事項は必要に応じて `TBD-*` とし、一般論やAIの推測だけで確定しない。
- ソースコードに存在するという理由だけで、偶然の実装詳細を外部仕様へ昇格させない。

## 仕様ID

- 外部仕様: `EXT-<AREA>-NNN`
- 内部設計: `INT-<AREA>-NNN`
- 受入条件: `AC-<AREA>-NNN`
- 未決事項: `TBD-NNN`

仕様IDは章立ての代わりではありません。人間が読みやすい設計書の中で、変更・レビュー・テストに必要な契約だけを識別します。

## 基本原則

- Java 8 / JRE 8 互換を維持する。
- 製品実行時依存は原則として Java SE 標準ライブラリだけとする。
- Maven をビルドの正本とし、IDE固有設定を正本にしない。
- 送信側から受信側への片方向通信を維持し、ACK/NACK、自動再送、フロー制御を暗黙に追加しない。
- シーケンス番号はファイル上の論理位置として扱い、順不同・重複を受信側で処理する。
- 1転送の全パケットは同じランダムセッションIDを使用し、別セッションのパケットを混在させない。
- メタデータのファイルサイズとチャンク条件を相互検証する。
- 受信完了は完成ファイルのSHA-256一致後にだけ成立させる。
- セッションIDは認証ではない。SHA-256も送信者認証、認可、暗号化、MAC、電子署名を提供しない。
- 受信側は送信元ファイル名をローカル保存先の決定に使用しない。
- 外部入力を検証する前にファイルや受信状態を変更しない。
- 将来拡張だけを理由に抽象化層、フレームワーク、互換層、設定項目を増やさない。
- 過去仕様や廃止済み構成は現行文書へ残さず、履歴はGit、Issue、PRに任せる。

## Java

- Java 8でコンパイル・実行できる構文とAPIだけを使う。
- `record`、仮想スレッド、`Path.of`、`InputStream.readNBytes` などJava 8より新しい機能を導入しない。
- 責務が増えていないのにクラスや抽象化を増やさない。ただし複数コンポーネントで同じ責務を共有する場合は、小さな専用クラスへ分離してよい。
- Javaはスペース4文字、XML/YAMLはスペース2文字、UTF-8/LFとする。共有書式は `.editorconfig` に従う。
- コメントはコードから分からない理由・制約を書く。過去実装の説明は書かない。

## 文書

- 本文、章名、表見出しは自然な日本語を基本とする。
- コマンド名、クラス名、API名、実際のプロトコル項目など、識別子として原文を維持する方が正確なものはそのまま使う。
- root READMEは利用者向け概要と使い方に限定し、詳細仕様や開発手順の正本にしない。
- EXTERNAL_DESIGNは利用者・通信相手・ファイル・標準入出力から観測できる契約を書く。
- INTERNAL_DESIGNは責務、状態、処理方式、並行処理、リソース所有、エラー伝播、不変条件を書く。
- OPERATIONSはビルド、テスト、リリース方法を書く。
- ADRは仕様を重複記載するのではなく、重要な判断の理由と見直し条件を書く。本文と見出しは他の設計文書と同じ日本語方針に従う。

## テスト

- 網羅率の数字より、CLI契約、wireの既知byte列、メタデータ検証、セッション分離、チャンク数上限、SHA-256検証、順不同、重複、欠損、ファイル境界、不正パケット隔離、ライフサイクルを優先する。
- encoder/decoder同士の往復だけでwire formatを検証せず、既知byte列または独立したraw packetを使う。
- 受入テストはprivate実装ではなくCLI、UDP socket、ファイル、stdout/stderrなどの観測結果を検証する。
- UDP integration testはloopbackだけを使う。
- 実ネットワークの偶然のpacket lossや固定sleepへ依存せず、raw packet送信や観測可能な状態待ちを使って決定的にテストする。
- 不具合修正では可能な限り再現テストを追加する。

## GitHub 上の変更

- 挙動・構造変更はIssueとbranch/PRを基本とする。
- Issue、PR、commit説明は日本語で書く。
- 無関係なリファクタリングを同じPRへ混ぜない。ただし仕様、テスト、実装、文書を一致させるため不可分な整理はまとめてよい。
- ユーザーの明示指示なしにPRをマージしない。

## Definition of Done

1. 外部挙動の変更を外部設計と受入条件へ先に反映した。
2. 必要な内部設計とADRを更新した。
3. `EXT -> AC -> INT -> test -> production component` を追跡できる。
4. Java 8 / Java 25 の `mvn -B -ntp verify` がCIで成功する。
5. 実行可能JARと配布ZIPが生成できる。
6. README、設計書、OPERATIONS、ADR、テスト、実装に矛盾がない。
7. 変更後に不要なコード、テスト、文書を残さない。
8. PRの差分がIssueの目的に閉じている。
